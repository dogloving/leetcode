```CPP
class Solution {
public:
    int subtractProductAndSum(int n) {
        int p = 1, s = 0;
        while (n) {
            int x = n%10;
            n /= 10;
            p *= x;
            s += x;
        }
        return p-s;
    }
};
```

题意：整数n，求他每一位上的积与他每一位上的和之差。