```CPP
class Solution {
public:
    string toLowerCase(string str) {
        string res;
        for (char ch: str) {
            if (ch >= 'A' && ch <= 'Z') ch += 32;
            res.push_back(ch);
        }
        return res;
    }
};
```

题意：将字符串str转成全部小写。